#include <stdio.h>
#include <string.h>
int main()
{
    char a[100000];
    scanf("%s", a);
    int sum = 0;
    sum = sum + a;
    printf("%s\n", a);
    // for (int i = 0; i < a; i++)
    // {
    //     printf("%c", a[i]);
    // }

    return 0;
};